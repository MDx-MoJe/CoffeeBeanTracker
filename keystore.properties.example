# 复制本文件为 keystore.properties 并填入真实值。
# keystore.properties 已被 .gitignore 忽略，绝不进 git（密钥与密码只存在本地）。
storeFile=keystore.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=coffee_key
keyPassword=YOUR_KEY_PASSWORD
